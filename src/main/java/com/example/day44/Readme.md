# 最长递增子序列 300
递增子序列不要求在数组中是连续的
dp[i]表示以nums[i]为结尾的连续子数组的定义
dp[i] = max(dp[i],dp[j] + 1)
长度至少都是1
# 最长重复子数组 718
就是求最长公共部分，最长的长度找出来
1. 二维dp数组可以表示所有状态
2. dp[i][j] 以i - 1，j - 1为结尾的nums的最大子数组的长度
3. if(nums1[i - 1] == nums2[j - 1])dp[i][j] = dp[i - 1][j - 1] + 1
4. for i = 1 ;i <= nums1.size;i ++
5. for j = 1 ;j <= nums2.size ;j ++

ma = max(ma,dp[i][j])
6. 二维dp数组全部遍历找出最大结果才是要求的结果